# Sentry - AI-Powered Log Anomaly Detection
# Environment Configuration Template

# === REQUIRED ENVIRONMENT VARIABLES ===

# Database Configuration (PostgreSQL)
DATABASE_URL=postgresql://username:password@host:port/database

# Session Security
SESSION_SECRET=your_super_secure_random_session_secret_here_min_32_chars

# Replit Authentication (for Google OAuth)
REPL_ID=your_repl_id
REPLIT_DEPLOYMENT_TOKEN=your_deployment_token

# === OPTIONAL ENVIRONMENT VARIABLES ===

# Application Configuration
NODE_ENV=production
PORT=5000

# File Upload Limits
MAX_FILE_SIZE=52428800
MAX_LOG_ENTRIES=100000

# CORS Configuration
CORS_ORIGIN=https://yourdomain.com

# Request Timeouts
UPLOAD_TIMEOUT=300000
PROCESSING_TIMEOUT=600000

# === NOTES ===
# 1. Copy this file to .env and fill in your actual values
# 2. Never commit .env files to version control
# 3. DATABASE_URL should point to a PostgreSQL 14+ database
# 4. SESSION_SECRET should be a random 32+ character string
# 5. For local development, you can use sqlite:// for DATABASE_URL
# 6. REPL_ID and REPLIT_DEPLOYMENT_TOKEN are required for OAuth to work